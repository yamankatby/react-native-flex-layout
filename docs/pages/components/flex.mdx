import PropsTable from "../../components/PropsTable";

# Flex

Flex is a container view extends React Native's flex-box properties and comes with helpful style shorthands.

## Import

```js
import { Flex, Spacer } from 'react-native-flex-layout';
```

- **Flex:** A `Box` with shorthands for `flex` properties.
- **Spacer:** Creates an adjustable, empty space that can be used to tune the spacing between child elements
within `Flex`.

## Usage

### Using the Flex

Here are some helpful shorthand props:

- `fill` is `flex` (If `true` is passed, the `flex` style property will be set to `1`).
- `direction` is `flexDirection`.
- `justify` is `justifyContent`.
- `items` is `alignItems`.
- `self` is `alignSelf`.
- `content` is `alignContent`.
- `wrap` is `flexWrap`.
- `basis` is `flexBasis`.
- `grow` is `flexGrow`.
- `shrink` is `flexShrink`.

While you can pass the verbose props, using the shorthand can save you some time.

### Using the Spacer

As an alternative to `Stack`, you can combine `Flex` and `Spacer` to create stackable and responsive layouts.

## Props API

<PropsTable
  props={[
    { name: 'fill', description: 'Shorthand for the `flex` style property.', type: 'boolean | number' },
    { name: 'inline', description: 'Shorthand for `{ flexDirection: "row" }`.', type: 'boolean' },
    {
      name: 'center',
      description: 'Shorthand for `{ justifyContent: "center", alignItems: "center" }`.',
      type: 'boolean'
    },
    {
      name: 'direction',
      description: 'Shorthand for the `flexDirection` style property.',
      type: 'row | column | row-reverse | column-reverse'
    },
    {
      name: 'justify',
      description: 'Shorthand for the `justifyContent` style property.',
      type: 'start | end | center | between | around | evenly'
    },
    {
      name: 'items',
      description: 'Shorthand for the `alignItems` style property.',
      type: 'start | end | center | stretch | baseline'
    },
    {
      name: 'self',
      description: 'Shorthand for the `alignSelf` style property.',
      type: 'auto | start | end | center | stretch | baseline'
    },
    {
      name: 'content',
      description: 'Shorthand for the `alignContent` style property.',
      type: 'start | end | center | stretch | between | around'
    },
    {
      name: 'wrap',
      description: 'Shorthand for the `flexWrap` style property.',
      type: 'boolean | wrap | nowrap | wrap-reverse'
    },
    { name: 'basis', description: 'Shorthand for the `flexBasis` style property.', type: 'auto | content | number' },
    { name: 'grow', description: 'Shorthand for the `flexGrow` style property.', type: 'number' },
    { name: 'shrink', description: 'Shorthand for the `flexShrink` style property.', type: 'number' },
  ]}
/>

`...BoxProps`
